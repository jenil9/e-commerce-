<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Profile</title>
  <style>
    body {
      background-color: #f0f4f8;
      font-family: 'Segoe UI', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .card {
      background: white;
      padding: 2rem;
      border-radius: 12px;
      box-shadow: 0 6px 18px rgba(0,0,0,0.1);
      width: 400px;
    }
    .card h2 {
      margin-bottom: 1rem;
      color: #1e293b;
    }
    .info {
      margin-bottom: 1rem;
    }
    .info strong {
      color: #0f172a;
    }
    .btn {
      display: inline-block;
      margin-top: 1.5rem;
      padding: 0.6rem 1.2rem;
      background: #3b82f6;
      color: white;
      text-decoration: none;
      border-radius: 8px;
    }
    .btn:hover {
      background: #2563eb;
    }
  </style>
</head>
<body>
  <div class="card">
    <h2><%= user.name %>'s Profile</h2>
    <div class="info"><strong>Full Name:</strong> <%= user.name %> <%= user.lastname %></div>
    <div class="info"><strong>Email:</strong> <%= user.email %></div>
    <div class="info"><strong>Created:</strong> <%= user.createdAt.toDateString() %></div>
    
    <a href="/home" class="btn">← Back to Home</a>
  </div>
 
</body>
</html>
